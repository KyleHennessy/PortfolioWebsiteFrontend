import { Fragment } from 'react';

import ProjectsList from './ProjectsList.js';

const Projects = () => {
    return (
        <Fragment>
            <ProjectsList />
        </Fragment>
    );
};

export default Projects;
